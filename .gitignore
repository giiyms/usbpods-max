# this will handle to macos hidden folders
# ignore all starts with .* except .git and so on
.*
!.git*
!.editorconfig
!.vscode/launch.json
!.vscode/settings.json

# build folder
/build*

# keep GitHub metadata and Pages marker
!.github
!.github/**
!.nojekyll
!web/.nojekyll